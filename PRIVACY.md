# Privacy

Margin stores notes locally in an encrypted SQLite database. The encryption key is stored in macOS Keychain.

Optional folder sync is off by default. When enabled, Margin writes readable Markdown files to the folder selected by the user; those synchronized copies are not encrypted by Margin. Margin checks GitHub Releases for app updates unless automatic updates are disabled in Settings. Margin does not include analytics, advertising, or telemetry.
